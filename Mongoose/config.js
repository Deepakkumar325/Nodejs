const mongoose = require('mongoose');
mongoose.connect("mongodb://locolhost:27017/deepak");

// here we create a mongoose connection so that we can't create connection we only import this 
//lec 3